---
description: 依赖安装白名单、避免重复造轮子查阅规范 (修改 package.json 或引入新模块时触发)
globs: package.json, src/**/*.ts
---
# 📦 技术栈与依赖白名单 (Tech Stack & DRY)

- **写代码前查阅**: 拒绝重复造轮子。编写通用逻辑前，先检索 `src/shared/` 下是否有现成工具。
- **零容忍滥装包**: 严禁擅自使用 `npm install` 引入 `lodash`, `moment`, `axios` 等多余包，请用现代原生 ES6+ 实现。
- **强制技术栈**:
  - 路由与参数: `cac`
  - 终端交互: `@clack/prompts` (严禁 inquirer)
  - 终端高亮: `picocolors` (严禁 chalk)
  - 动画加载: `ora`
  - 命令执行: `execa`
  - Shell 解析: `shell-quote`
  - 目录扫描: `fast-glob` + `ignore`
  - LLM 核心: 官方 Vercel `ai` SDK + `openai` + `zod`
  - 存储与配置: `better-sqlite3` + `conf` + `dotenv`