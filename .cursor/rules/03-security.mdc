---
description: 涉及系统命令执行、AST解析、文件读写时的绝对安全红线
globs: src/**/*.ts
---
# 🛡️ 绝对安全红线 (Security Red Lines)

- **防注入 (No Shell Injection)**: 永远禁止使用 `child_process.exec` 或开启 `shell: true`。必须通过 `execa('cmd', ['args'])` 数组形式隔离执行大模型生成的命令。
- **AST 解析优于正则**: 评估大模型生成的命令是否危险时，必须使用 `shell-quote` 将命令解析为 AST 树，精准识别高危二进制文件 (`rm`, `kill`, `chmod`, `sudo`) 及操作符 (`>`, `|`)，拒绝简单正则。
- **高危操作强制拦截**: 任何修改状态、删除文件、网络提权的操作，默认高危。必须挂起进程，通过 `@clack/prompts` 强制用户确认或用 `clipboardy` 推入剪贴板，绝对不可在 Auto 模式下静默执行。
- **防 Token 爆破**: `fs_read` 等工具必须在底层硬编码最大截断长度 (如 `slice(0, 8000)`) 和最大扫描深度 (`depth: 2`)。